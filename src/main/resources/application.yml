spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/springsecuritydemo}
    username: ${DB_USER:root}
    password: ${DB_PASSWORD:spring-security}
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
